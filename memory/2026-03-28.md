- 在群組「門市已開支票及發票」收到操作規則：備註欄預設只可填 `-`，只有使用者或店舖明確提供特別說明時才可寫入，不可自行推測。
- Google Sheet backend 已由使用者確認可正常寫入；若日後回報寫入失敗，必須提供具體錯誤原因，不可只說「寫入唔到」。
- 使用者表示已按昨日指示把最新 Google Apps Script 貼上並重新部署 Web App；其後已由本機再次直接 POST 測試寫入成功（Web App 回傳 `ok: true`，測試列含 `PU003 / TEST / TEST-WRITE-20260328-002 / INV-TEST-20260328-002`），代表重新部署後 backend 仍然可用。
- 使用者明確要求：之後所有回覆、workflow、以及準備跨機使用的 skill 內容，一律使用繁體中文撰寫；不可只靠當前機器／session 的語言偏好。
- 已在 workspace 建立可攜式繁體中文 skill 草稿 `skills/cheque-whatsapp-sheet/`，採保守版設計：把 WhatsApp 支票／發票流程、欄位規則、確認規則、Google Sheet 寫入規則寫入 skill 與 references；backend 敏感設定不硬編碼進 skill。
- 已完成 WhatsApp 圖片 inbound mapping 驗證：附件實體檔案存在於 `C:\Users\victor\.openclaw\media\inbound\4ba659d6-32e8-488d-adb0-b150be440ac9.jpg`，而 `C:\Users\victor\.openclaw\agents\main\sessions\ff6e1c9b-1c9f-4db6-8eed-9d8ec27f5234.jsonl:157` 有直接記錄同一張圖的附件路徑、`message_id` `3EB05AB1C8A99887B746E6`、sender metadata，以及 `<media:image>` / image payload。
- 已確認 OpenClaw 對 WhatsApp 圖片的處理路徑可視為正常：收到訊息後會把媒體落地到 `media/inbound`，並在 session JSONL 中保留可對應的引用資料。
- 根據上述驗證，已可較有把握評估「WhatsApp 圖片 → 自動 upload 至 Google Drive → 將圖片連結／預覽寫入 Google Sheet」技術上可行；下一步若正式實作，需補設計 Sheet 新欄位（至少 `圖片連結`，可選 `圖片預覽`）及更新 workflow / skill / Apps Script。
- 已完成 V1 圖片方案 groundwork：workflow、skill、Apps Script `appendMain/updateImage`、本地壓縮腳本、Drive upload worker 骨架均已建立；其中 Apps Script 已實測 `appendMain` 與 `updateImage` 成功，圖片壓縮測試將樣本壓至約 157KB。
- 使用者表示 Synology NAS 可作為 Google Drive 以外的圖片存放替代方案，但目前決定先把 NAS 話題記錄在案、暫不繼續實作，待之後再決定是否走 Synology 方案。
- 使用者已正式授權助手以更高自由度重構整體支票流程，不再只做零碎補丁；方向改為建立更完整、易用、可追查的「支票中台」而非單靠 Google Sheet。
- 已落地 V2 初版 scaffold：新增 `CHEQUE-SYSTEM-V2.md`、`CHEQUE-SYSTEM-V2-USAGE.md` 與 `cheque-system/scripts/cheque-ledger.mjs`，以 workspace 內 JSON store + JSONL event log 作本地 system of record；已實測可初始化、upsert 記錄、確認、匯出 Google Sheet `appendMain` payload。
- 已繼續把 V2 由概念推進到可執行：新增 `cheque-system/scripts/lib/ledger-store.mjs`、`cheque-system/scripts/cheque-sheet-sync.mjs`、`cheque-system/scripts/cheque-image-sync.mjs`、`cheque-system/scripts/cheque-process-record.mjs`。現已實測可由 ledger 單筆寫入 Google Sheet、把 inbound 圖片壓縮至約 157KB，並以一條 orchestrator 命令完成「寫 Sheet + 本地壓圖」流程。
- 已新增並啟用 workspace hook `hooks/cheque-auto-intake/`，OpenClaw log 顯示已註冊為 `message:preprocessed` hook，會自動將符合條件的 WhatsApp 支票訊息 intake 到本地 ledger；同時新增 `cheque-system/scripts/cheque-status-report.mjs` 與 `CHEQUE-READY-TO-TEST.md` 作第一輪實戰測試入口。
- 第一輪真實測試已成功：使用者以 WhatsApp 實際提交兩筆資料（`TSW01 跨境支票`、`TSW02 鋪頭支票`），助手成功完成抽取、群內確認、Google Sheet 寫入，以及本地 ledger / 圖片壓縮記錄；使用者亦已確認在 Google Sheet 內看見兩筆成功資料。
- 使用者確認支票流程現可進入正式試用階段；後續以 live workflow 標準持續使用，再逐步優化自動 intake、自動狀態流轉，以及圖片最終雲端存放方案。
- 使用者對將系統升級成 PWA 表示興趣，並授權先做一個可試看的模板。已建立 `cheque-pwa-template/` 靜態 PWA 原型，包含店舖版、管理員版、會計版三個角色畫面；本機預覽已可於 `http://127.0.0.1:4173` 開啟，用作第一輪介面方向驗證。
- 使用者已批准自動推進 PWA 方案，並新增銀行對帳輸入要求：未來不只 CSV / Excel，亦要預留 PDF 或圖片作為銀行資料輸入來源。已把此要求納入 `PWA-PHASE1-FUNCTIONS.md` 的正式第一期規劃。
- 使用者再次明確授權助手持續自動推進整個支票系統與 PWA 開發，不需逐步等待批准；後續以「做到可試 / 有成果就匯報」方式持續迭代。
- 已把 `cheque-pwa-template/` 由靜態假畫面升級成真資料 preview：`preview-server.mjs` 新增 `/api/dashboard` 會直接讀取 `cheque-system/data/records.json`，前端 `app.js` 會渲染店舖版最近記錄、管理員統計／列表、會計待過數清單，並可點開單筆詳情 drawer。新的外網臨時預覽連結可用於手機驗證真資料畫面。
- 群組「門市已開支票及發票」已正式要求一律按 `CHEQUE-WHATSAPP-WORKFLOW.md` 最新常駐規則處理：必須使用固定繁體中文列點摘要、只接受原提交者以 `1 / 確定 / OK / 好` 作最終確認、同時間多張單不可憑單一句 `OK` 直接入表、資料不足時必須明確列缺欄位，成功寫入只回 `已成功儲存`。
- 今天已在該群組按新流程完成至少 3 筆真實入表：`PU003 鋪頭支票`（更正後金額 `2715.30`、日期 `2026-04-07`、record key `3AD09DFB36877942EB3E`）、`PU003 鋪頭支票`（支票號碼 `067174`、金額 `1860.00`、record key `3AE71265C053381557C7`）、`YL001 鋪頭支票`（原提交者為輝將軍；他更正支票號碼為 `096406` 後自行確認，record key `3A7A209720C442A5F528`）。
- 已驗證「非原提交者不可最終確認」規則在群組實戰有效：Chun 曾替輝將軍的 `YL001 鋪頭支票` 回 `1`，助手依規則拒絕入表，待輝將軍本人回 `1` 後才寫入 Google Sheet。
- 群組成員今天對流程做了真實壓力測試：會穿插玩笑、貼圖、語音、辱罵式口吻；在群聊環境中應保持簡短、有點人味，但避免對貼圖/閒聊過度回覆，必要時可 `NO_REPLY` 或只短答，以免洗版。
- 今天開始出現「現金單」實際使用需求；現況可先按支票類別 `現金單` 處理，但欄位規則尚未完全定稿。臨時做法是：若無支票日期及支票號碼，先以 `-` 處理，並要求使用者明確提供門市編號、金額、收款公司名、Invoice No.；之後應把現金單專用欄位規則正式寫入 workflow/skill，避免再靠臨時判斷。
