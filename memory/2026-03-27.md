# 2026-03-27

- 使用者交代：現有系統導入格式是 `.xls`。
- 使用者希望之後可在手機用 WhatsApp / Telegram 發相片配合流程。
- 有一千多間供應商資料；系統可以導出 Excel 給助手處理。
- 目前尚未拿到匯入模板；此項目之後再做，先記住需求與限制。
- 使用者目前暫時不處理 PO 單匯入，改為優先規劃 WhatsApp 群組流程，用來記錄各鋪已開支票與對應供應商發票。
- 新流程重點：由鋪頭同事在 WhatsApp 群組發送支票／發票相片，助手負責從圖片整理資料並填回 Excel，不要求前線同事手動逐欄輸入。
- 使用者接受支票與發票疊在一起拍照；關鍵是 Excel 只需記錄核心欄位，而非完整發票資料。
- Excel 核心欄位已明確：門市／鋪頭、支票號碼、支票金額、收款公司名（供應商名）、對應 Invoice No.；發票日期不需要。
- 公司全部鋪頭均使用恆生銀行支票；支票號碼規則已確認為支票底部那行中的 6 位數字，這是後續抽取 cheque no. 的重要 business rule。
- 門市資訊可能未必能從文件照片穩定辨識，之後較適合由 WhatsApp 訊息 caption 補充門市代號／名稱。
- 對於支票與發票同圖、金額不一致的情況，不應假設一張支票必然對應一張發票；日後流程／表格需容許一對多或僅記錄 invoice no. 關聯。
- 已正式定稿 WhatsApp 支票／發票記錄流程：門市同事於群組發送「門市編號 + 支票類別 + 圖片」，助手以短版格式回覆，使用者以 `1/確定/OK/好` 確認、以 `2/更改/修改/有錯` 要求修改，確認後寫入 Google Sheet。
- 目前正式欄位順序為：門市編號、支票類別、輸入日期、支票付款日期、支票號碼、支票金額、收款公司名、Invoice No.、備註。
- 已建立固定流程文件 `CHEQUE-WHATSAPP-WORKFLOW.md`，作為之後 webchat / WhatsApp 可重讀的工作規格。
- 已建立 Google Sheet 並可公開讀取；Apps Script Web App 已部署並測試成功寫入，後續仍需留意中文編碼與跨 session 規則同步問題。
- `CHEQUE-WHATSAPP-WORKFLOW.md` 已進一步補強最新群組行為規格：群內用短版列點回覆、固定 `1.確定 2.更改`、多單待確認時不可憑單獨 `OK` 寫入、只有原提交者可作最終確認、資料不足時要列出缺欄並要求補相、寫入結果只回 `已成功儲存 / 不成功儲存`。
- 之後再補強：備註欄預設必須為 `-`，只有使用者或店舖明確提供額外說明時才可填；助手不得自行推測備註內容。另已由本機直接測試 Apps Script 寫入成功（測試列：`PU003 / TEST / TEST-WRITE-20260328-001`），表示 Google Sheet backend 通路可用，群組問題較大機會在 session / workflow 未真正觸發寫入。
- 已將支票／發票 WhatsApp 記錄流程升級寫入 `AGENTS.md` 常駐規則：凡遇到支票、發票、Google Sheet 寫入、門市支票欄位整理、Invoice 對應等任務，須先讀 `CHEQUE-WHATSAPP-WORKFLOW.md` 並以其為準，不可退回舊格式。
